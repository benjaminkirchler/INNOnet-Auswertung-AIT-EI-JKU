# R
.Rhistory
.RData
.Rproj.user/

# Data (kept local only, not pushed)
data/

# Output
output/*
!output/
!output/models/
!output/**/.keep
output/models/*.rds

# LaTeX build artifacts
*.aux
*.log
*.out
*.bbl
*.blg
*.bcf
*.run.xml
*.synctex.gz
*.fls
*.fdb_latexmk
*.toc
*.lof
*.lot
main.pdf

# renv library (keep renv.lock and settings, ignore the local library)
renv/library/
renv/local/
renv/staging/

# OS / editor
.DS_Store
Thumbs.db

# AI/agent tooling artifacts (kept locally, not pushed)
CLAUDE.md
.claude/
AGENT_LOG.md
IMPLEMENTATION_PLAN.md
TODO.md

# R scripts are part of the reproducible report pipeline and are versioned.
